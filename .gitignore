test.ipynb
.DS_STORE

# Visual Studio and Visual Studio Code
bin/
obj/
.vs/
.vscode/

# build, distribute, and bins
build/
!sdk_v2/cpp/nuget/build/
build_*/
.build_debug/*
.build_release/*
dist/
*.egg-info

# python
__pycache__/
*.py[cod]
*$py.class
**/.venv

# node
node_modules/
packages/
package-lock.json
# sdk_v2/js commits its lockfile to give CI deterministic, faster restores
# via `npm ci`. v1/samples/www intentionally float, so the global rule above
# stays in place and this negation re-includes only the v2 SDK lockfile.
!/sdk_v2/js/package-lock.json
prebuilds/

# Rust build targets
target/
Cargo.lock

# Build outputs
bin/
obj/
/src/cs/samples/ConsoleClient/test.http
logs/

# Local NuGet packages built from source
local-packages/

# JS SDK downloaded native core binaries
sdk/js/foundry-local-core/

# sdk_v2/js pack-prebuilds.mjs stages deps_versions.json into sdk_v2/js/
# at publish time so the tarball can carry it. The repo source of truth
# stays at sdk_v2/deps_versions.json; the copy is generated.
/sdk_v2/js/deps_versions.json
