# Build + test + pack stages for the sdk_v2 JS SDK.
#
# Unlike Python and C#, the JS SDK ships a single multi-platform npm tarball.
# Each per-platform build stage produces a `js-prebuild-<rid>` artifact
# containing prebuilds/<plat>-<arch>/ (two .node addons + the matching
# foundry_local shared library). The final `js_pack` stage downloads all
# five prebuild artifacts, merges them under sdk_v2/js/prebuilds/, stamps
# the version, and runs `npm pack` to produce a single .tgz.
#
# Build stages depend on:
#   * compute_version          (sdkVersion.txt for the eventual pack stamp)
#   * cpp_build_<rid>          (matching native foundry_local lib + import lib)
#
# Tests run on the same platform as the build.

stages:

# =====================================================================
# Build stages
# =====================================================================

- stage: js_build_win_x64
  displayName: 'JS SDK: Build Windows x64'
  dependsOn:
  - compute_version
  - cpp_build_win_x64
  jobs:
  - job: build
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    variables:
    - group: FoundryLocal-ESRP-Signing
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'cpp-native-win-x64'
        targetPath: '$(Pipeline.Workspace)/cpp-native-win-x64'
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-prebuild-win-x64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-prebuild'
    steps:
    - checkout: self
      clean: true
    - template: steps-build-js.yml
      parameters:
        rid: 'win-x64'
        nativeArtifactDir: '$(Pipeline.Workspace)/cpp-native-win-x64'
        outputDir: '$(Build.ArtifactStagingDirectory)/js-prebuild'
        signWindows: true

- stage: js_build_win_arm64
  displayName: 'JS SDK: Build Windows ARM64'
  dependsOn:
  - compute_version
  - cpp_build_win_arm64
  jobs:
  - job: build
    pool:
      name: onnxruntime-medium-arm64-scus
      os: windows
      hostArchitecture: arm64
    variables:
    - group: FoundryLocal-ESRP-Signing
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'cpp-native-win-arm64'
        targetPath: '$(Pipeline.Workspace)/cpp-native-win-arm64'
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-prebuild-win-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-prebuild'
    steps:
    - checkout: self
      clean: true
    - template: steps-build-js.yml
      parameters:
        rid: 'win-arm64'
        nativeArtifactDir: '$(Pipeline.Workspace)/cpp-native-win-arm64'
        outputDir: '$(Build.ArtifactStagingDirectory)/js-prebuild'
        signWindows: true

- stage: js_build_linux_x64
  displayName: 'JS SDK: Build Linux x64'
  dependsOn:
  - compute_version
  - cpp_build_linux_x64
  jobs:
  - job: build
    pool:
      name: onnxruntime-Ubuntu2404-AMD-CPU
      os: linux
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'cpp-native-linux-x64'
        targetPath: '$(Pipeline.Workspace)/cpp-native-linux-x64'
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-prebuild-linux-x64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-prebuild'
    steps:
    - checkout: self
      clean: true
    - template: steps-build-js.yml
      parameters:
        rid: 'linux-x64'
        nativeArtifactDir: '$(Pipeline.Workspace)/cpp-native-linux-x64'
        outputDir: '$(Build.ArtifactStagingDirectory)/js-prebuild'

- stage: js_build_linux_arm64
  displayName: 'JS SDK: Build Linux ARM64'
  dependsOn:
  - compute_version
  - cpp_build_linux_arm64
  jobs:
  - job: build
    pool:
      name: onnxruntime-linux-ARM64-CPU-2019
      os: linux
      hostArchitecture: arm64
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'cpp-native-linux-arm64'
        targetPath: '$(Pipeline.Workspace)/cpp-native-linux-arm64'
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-prebuild-linux-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-prebuild'
    steps:
    - checkout: self
      clean: true
    - template: steps-build-js.yml
      parameters:
        rid: 'linux-arm64'
        nativeArtifactDir: '$(Pipeline.Workspace)/cpp-native-linux-arm64'
        outputDir: '$(Build.ArtifactStagingDirectory)/js-prebuild'

- stage: js_build_osx_arm64
  displayName: 'JS SDK: Build macOS ARM64'
  dependsOn:
  - compute_version
  - cpp_build_osx_arm64
  jobs:
  - job: build
    pool:
      name: AcesShared
      os: macOS
      demands:
      - ImageOverride -equals ACES_VM_SharedPool_Sequoia
    variables:
    - group: FoundryLocal-ESRP-Signing
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'cpp-native-osx-arm64'
        targetPath: '$(Pipeline.Workspace)/cpp-native-osx-arm64'
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-prebuild-osx-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-prebuild'
    steps:
    - checkout: self
      clean: true
    - template: steps-build-js.yml
      parameters:
        rid: 'osx-arm64'
        nativeArtifactDir: '$(Pipeline.Workspace)/cpp-native-osx-arm64'
        outputDir: '$(Build.ArtifactStagingDirectory)/js-prebuild'
        signMac: true

# ================================================================
# Test stages - same platform only.
# ================================================================

- stage: js_test_win_x64
  displayName: 'JS SDK: Test Windows x64'
  dependsOn:
  - js_build_win_x64
  jobs:
  - job: test
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'js-prebuild-win-x64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-win-x64'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/fetch-test-data-from-blob.yml@self
    - template: steps-test-js.yml
      parameters:
        rid: 'win-x64'
        prebuildArtifactDir: '$(Pipeline.Workspace)/js-prebuild-win-x64'
        testDataSharedDir: '$(Build.SourcesDirectory)/test-data-shared'

- stage: js_test_win_arm64
  displayName: 'JS SDK: Test Windows ARM64'
  dependsOn:
  - js_build_win_arm64
  jobs:
  - job: test
    pool:
      name: onnxruntime-medium-arm64-scus
      os: windows
      hostArchitecture: arm64
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'js-prebuild-win-arm64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-win-arm64'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/fetch-test-data-from-blob.yml@self
      parameters:
        scriptType: ps
    - template: steps-test-js.yml
      parameters:
        rid: 'win-arm64'
        prebuildArtifactDir: '$(Pipeline.Workspace)/js-prebuild-win-arm64'
        testDataSharedDir: '$(Build.SourcesDirectory)/test-data-shared'

- stage: js_test_linux_x64
  displayName: 'JS SDK: Test Linux x64'
  dependsOn:
  - js_build_linux_x64
  jobs:
  - job: test
    pool:
      name: onnxruntime-Ubuntu2404-AMD-CPU
      os: linux
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'js-prebuild-linux-x64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-linux-x64'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/fetch-test-data-from-blob.yml@self
    - template: steps-test-js.yml
      parameters:
        rid: 'linux-x64'
        prebuildArtifactDir: '$(Pipeline.Workspace)/js-prebuild-linux-x64'
        testDataSharedDir: '$(Build.SourcesDirectory)/test-data-shared'

- stage: js_test_linux_arm64
  displayName: 'JS SDK: Test Linux ARM64'
  dependsOn:
  - js_build_linux_arm64
  jobs:
  - job: test
    pool:
      name: onnxruntime-linux-ARM64-CPU-2019
      os: linux
      hostArchitecture: arm64
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'js-prebuild-linux-arm64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-linux-arm64'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/fetch-test-data-from-blob.yml@self
    - template: steps-test-js.yml
      parameters:
        rid: 'linux-arm64'
        prebuildArtifactDir: '$(Pipeline.Workspace)/js-prebuild-linux-arm64'
        testDataSharedDir: '$(Build.SourcesDirectory)/test-data-shared'

- stage: js_test_osx_arm64
  displayName: 'JS SDK: Test macOS ARM64'
  dependsOn:
  - js_build_osx_arm64
  jobs:
  - job: test
    pool:
      name: AcesShared
      os: macOS
      demands:
      - ImageOverride -equals ACES_VM_SharedPool_Sequoia
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'js-prebuild-osx-arm64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-osx-arm64'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/fetch-test-data-from-blob.yml@self
    - template: steps-test-js.yml
      parameters:
        rid: 'osx-arm64'
        prebuildArtifactDir: '$(Pipeline.Workspace)/js-prebuild-osx-arm64'
        testDataSharedDir: '$(Build.SourcesDirectory)/test-data-shared'

# =====================================================================
# Pack stage — runs after all five builds. Pure packaging on Windows.
# =====================================================================

- stage: js_pack
  displayName: 'JS SDK: Pack multi-platform npm tarball'
  dependsOn:
  - js_build_win_x64
  - js_build_win_arm64
  - js_build_linux_x64
  - js_build_linux_arm64
  - js_build_osx_arm64
  jobs:
  - job: pack
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'js-prebuild-win-x64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-win-x64'
      - input: pipelineArtifact
        artifactName: 'js-prebuild-win-arm64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-win-arm64'
      - input: pipelineArtifact
        artifactName: 'js-prebuild-linux-x64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-linux-x64'
      - input: pipelineArtifact
        artifactName: 'js-prebuild-linux-arm64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-linux-arm64'
      - input: pipelineArtifact
        artifactName: 'js-prebuild-osx-arm64'
        targetPath: '$(Pipeline.Workspace)/js-prebuild-osx-arm64'
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-sdk-v2'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-sdk-v2'
    steps:
    - checkout: self
      clean: true
    - template: steps-pack-js.yml
      parameters:
        outputDir: '$(Build.ArtifactStagingDirectory)/js-sdk-v2'

# =====================================================================
# Package test — install the tarball as a clean npm consumer
# =====================================================================

- stage: js_package_test
  displayName: 'JS SDK: Test packaged tarball'
  dependsOn:
  - js_pack
  jobs:
  - job: test
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'js-sdk-v2'
        targetPath: '$(Pipeline.Workspace)/js-sdk-v2'
    steps:
    - checkout: self
      clean: true
    - task: NodeTool@0
      displayName: 'Use Node.js 20'
      inputs:
        versionSpec: '20.x'
    - task: PowerShell@2
      displayName: 'Run packaged JS basic_chat sample'
      inputs:
        targetType: inline
        pwsh: true
        script: |
          $ErrorActionPreference = 'Stop'

          $package = Get-ChildItem '$(Pipeline.Workspace)/js-sdk-v2' -Recurse -Filter '*.tgz' |
            Select-Object -First 1
          if (-not $package) { throw 'JS SDK tarball not found' }

          $consumerDir = Join-Path '$(Agent.TempDirectory)' 'js-sdk-package-test'
          if (Test-Path $consumerDir) { Remove-Item -Recurse -Force $consumerDir }
          if (Test-Path $env:FOUNDRY_LOCAL_SAMPLE_CACHE_DIR) {
            Remove-Item $env:FOUNDRY_LOCAL_SAMPLE_CACHE_DIR -Recurse -Force
          }
          New-Item -ItemType Directory -Force -Path $consumerDir | Out-Null
          Set-Location $consumerDir

          npm init -y | Out-Null
          npm install $package.FullName --no-audit --no-fund
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

          $sample = Join-Path $consumerDir 'basic_chat.mjs'
          Copy-Item '$(Build.SourcesDirectory)/sdk_v2/js/examples/basic_chat.mjs' -Destination $sample -Force
          node $sample
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      env:
        FOUNDRY_LOCAL_SAMPLE_CACHE_DIR: '$(Agent.TempDirectory)/js-package-model-cache'
