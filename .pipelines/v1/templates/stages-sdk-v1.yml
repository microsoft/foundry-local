# sdk_v1 coordinator: emits the full legacy (sdk/) stage graph.
#
# Produces NuGet/wheel/JS artifacts for Foundry Local Core (FLC) plus the
# v1 C# / JS / Python / Rust SDKs in both base and WinML flavors.
#
# Assumes the caller has already emitted:
#   * a `compute_version` stage publishing the `version-info` pipeline
#     artifact (containing sdkVersion.v1.txt, pyVersion.v1.txt, flcVersion.v1.txt)
#   * any stage referenced by `gateDependsOn` (typically `detect_changes`)
#
# The entire subgraph is gated by a single artificial root stage (`v1_gate`)
# whose runtime condition is supplied via `gateCondition`. When v1_gate is
# skipped, every downstream v1 stage skips via ADO's default `succeeded()`
# cascade on dependsOn. See stages-sdk-v2.yml for the rationale behind this
# pattern.

parameters:
- name: version
  type: string
- name: prereleaseId
  type: string
  default: 'none'
- name: isRelease
  type: boolean
  default: false
- name: neutronServerBranch
  type: string
- name: gateCondition
  type: string
  default: 'succeeded()'
- name: gateDependsOn
  type: object
  default: []

stages:

# -- Artificial root: gates the entire v1 subgraph --
- stage: v1_gate
  displayName: 'sdk_v1 build gate'
  condition: ${{ parameters.gateCondition }}
  dependsOn: ${{ parameters.gateDependsOn }}
  jobs:
  - job: noop
    displayName: 'sdk_v1 build enabled'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    steps:
    - checkout: none
    - script: echo "sdk_v1 build enabled"
      displayName: 'sdk_v1 build enabled'

- stage: build_core
  displayName: 'Build Core'
  dependsOn:
  - v1_gate
  - compute_version
  jobs:
  - job: flc_win_x64
    displayName: 'win-x64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'flc-win-x64'
        targetPath: '$(Build.ArtifactStagingDirectory)/native'
    steps:
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: neutron-server
        ref: refs/heads/${{ parameters.neutronServerBranch }}
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-core-steps.yml
      parameters:
        flavor: win-x64
        platform: x64

  - job: flc_win_arm64
    displayName: 'win-arm64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'flc-win-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/native'
    steps:
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: neutron-server
        ref: refs/heads/${{ parameters.neutronServerBranch }}
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-core-steps.yml
      parameters:
        flavor: win-arm64
        platform: arm64

  - job: flc_linux_x64
    displayName: 'linux-x64'
    pool:
      name: onnxruntime-Ubuntu2404-AMD-CPU
      os: linux
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'flc-linux-x64'
        targetPath: '$(Build.ArtifactStagingDirectory)/native'
    steps:
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: neutron-server
        ref: refs/heads/${{ parameters.neutronServerBranch }}
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-core-steps.yml
      parameters:
        flavor: linux-x64
        platform: x64

  - job: flc_linux_arm64
    displayName: 'linux-arm64'
    pool:
      name: onnxruntime-linux-ARM64-CPU-2019
      os: linux
      hostArchitecture: arm64
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'flc-linux-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/native'
    steps:
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: neutron-server
        ref: refs/heads/${{ parameters.neutronServerBranch }}
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-core-steps.yml
      parameters:
        flavor: linux-arm64
        platform: arm64

  - job: flc_osx_arm64
    displayName: 'osx-arm64'
    pool:
      name: AcesShared
      os: macOS
      demands:
      - ImageOverride -equals ACES_VM_SharedPool_Sequoia
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'flc-osx-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/native'
    steps:
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: neutron-server
        ref: refs/heads/${{ parameters.neutronServerBranch }}
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    # AcesShared macOS agents don't have git-lfs pre-installed
    - script: brew install git-lfs && git lfs install
      displayName: 'Install Git LFS'
    - template: build-core-steps.yml
      parameters:
        flavor: osx-arm64
        platform: arm64

  - job: package_flc
    displayName: 'Package Core'
    dependsOn:
    - flc_win_x64
    - flc_win_arm64
    - flc_linux_x64
    - flc_linux_arm64
    - flc_osx_arm64
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      outputs:
      - output: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Build.ArtifactStagingDirectory)/flc-nuget'
      - output: pipelineArtifact
        artifactName: 'flc-wheels'
        targetPath: '$(Build.ArtifactStagingDirectory)/flc-wheels'
      - output: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Build.ArtifactStagingDirectory)/deps-versions'
    steps:
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: neutron-server
        ref: refs/heads/${{ parameters.neutronServerBranch }}
    - task: DownloadPipelineArtifact@2
      inputs:
        buildType: current
        artifactName: 'flc-win-x64'
        targetPath: '$(Pipeline.Workspace)/flc-win-x64'
    - task: DownloadPipelineArtifact@2
      inputs:
        buildType: current
        artifactName: 'flc-win-arm64'
        targetPath: '$(Pipeline.Workspace)/flc-win-arm64'
    - task: DownloadPipelineArtifact@2
      inputs:
        buildType: current
        artifactName: 'flc-linux-x64'
        targetPath: '$(Pipeline.Workspace)/flc-linux-x64'
    - task: DownloadPipelineArtifact@2
      inputs:
        buildType: current
        artifactName: 'flc-linux-arm64'
        targetPath: '$(Pipeline.Workspace)/flc-linux-arm64'
    - task: DownloadPipelineArtifact@2
      inputs:
        buildType: current
        artifactName: 'flc-osx-arm64'
        targetPath: '$(Pipeline.Workspace)/flc-osx-arm64'
    - task: PowerShell@2
      displayName: 'List downloaded platform artifacts'
      inputs:
        targetType: inline
        script: |
          foreach ($name in @('flc-win-x64','flc-win-arm64','flc-linux-x64','flc-linux-arm64','flc-osx-arm64')) {
            $dir = "$(Pipeline.Workspace)/$name"
            Write-Host "Contents of ${dir}:"
            if (Test-Path $dir) { Get-ChildItem $dir -Recurse | ForEach-Object { Write-Host $_.FullName } }
            else { Write-Host "  (directory not found)" }
          }
    - template: package-core-steps.yml
      parameters:
        version: ${{ parameters.version }}
        isRelease: ${{ parameters.isRelease }}
        prereleaseId: ${{ parameters.prereleaseId }}
        isWinML: false
        platforms:
        - name: win-x64
          artifactName: flc-win-x64
        - name: win-arm64
          artifactName: flc-win-arm64
        - name: linux-x64
          artifactName: flc-linux-x64
        - name: linux-arm64
          artifactName: flc-linux-arm64
        - name: osx-arm64
          artifactName: flc-osx-arm64

# ── Build C# SDK ──
- stage: build_cs
  displayName: 'Build C# SDK'
  dependsOn:
  - v1_gate
  - build_core
  jobs:
  - job: cs_sdk
    displayName: 'Build'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
      outputs:
      - output: pipelineArtifact
        artifactName: 'cs-sdk'
        targetPath: '$(Build.ArtifactStagingDirectory)/cs-sdk'
    steps:
    - checkout: self
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-cs-steps.yml
      parameters:
        version: ${{ parameters.version }}
        isRelease: ${{ parameters.isRelease }}
        prereleaseId: ${{ parameters.prereleaseId }}
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

# ── Build JS Node-API Addon (all platforms) ──
- stage: build_js_addon
  displayName: 'Build JS Addon'
  dependsOn: [v1_gate]
  jobs:
  - job: js_addon_win_x64
    displayName: 'Addon win32-x64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-addon-win32-x64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-addon'
    steps:
    - checkout: self
    - template: build-js-addon-steps.yml
      parameters:
        repoRoot: $(Build.SourcesDirectory)
    - task: PowerShell@2
      displayName: 'Stage addon artifact'
      inputs:
        targetType: inline
        script: |
          $destDir = "$(Build.ArtifactStagingDirectory)/js-addon"
          New-Item -ItemType Directory -Path $destDir -Force | Out-Null
          Copy-Item "$(Build.SourcesDirectory)/sdk/js/prebuilds/win32-x64/foundry_local_napi.node" "$destDir/foundry_local_napi.node" -Force

  - job: js_addon_win_arm64
    displayName: 'Addon win32-arm64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-addon-win32-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-addon'
    steps:
    - checkout: self
    - template: build-js-addon-steps.yml
      parameters:
        repoRoot: $(Build.SourcesDirectory)
        targetArch: arm64
    - task: PowerShell@2
      displayName: 'Stage addon artifact'
      inputs:
        targetType: inline
        script: |
          $destDir = "$(Build.ArtifactStagingDirectory)/js-addon"
          New-Item -ItemType Directory -Path $destDir -Force | Out-Null
          Copy-Item "$(Build.SourcesDirectory)/sdk/js/prebuilds/win32-arm64/foundry_local_napi.node" "$destDir/foundry_local_napi.node" -Force

  - job: js_addon_linux_x64
    displayName: 'Addon linux-x64'
    pool:
      name: onnxruntime-Ubuntu2404-AMD-CPU
      os: linux
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-addon-linux-x64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-addon'
    steps:
    - checkout: self
    - template: build-js-addon-steps.yml
      parameters:
        repoRoot: $(Build.SourcesDirectory)
    - task: PowerShell@2
      displayName: 'Stage addon artifact'
      inputs:
        targetType: inline
        script: |
          $destDir = "$(Build.ArtifactStagingDirectory)/js-addon"
          New-Item -ItemType Directory -Path $destDir -Force | Out-Null
          Copy-Item "$(Build.SourcesDirectory)/sdk/js/prebuilds/linux-x64/foundry_local_napi.node" "$destDir/foundry_local_napi.node" -Force

  - job: js_addon_linux_arm64
    displayName: 'Addon linux-arm64'
    pool:
      name: onnxruntime-linux-ARM64-CPU-2019
      os: linux
      hostArchitecture: arm64
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-addon-linux-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-addon'
    steps:
    - checkout: self
    - template: build-js-addon-steps.yml
      parameters:
        repoRoot: $(Build.SourcesDirectory)
    - task: PowerShell@2
      displayName: 'Stage addon artifact'
      inputs:
        targetType: inline
        script: |
          $destDir = "$(Build.ArtifactStagingDirectory)/js-addon"
          New-Item -ItemType Directory -Path $destDir -Force | Out-Null
          Copy-Item "$(Build.SourcesDirectory)/sdk/js/prebuilds/linux-arm64/foundry_local_napi.node" "$destDir/foundry_local_napi.node" -Force

  - job: js_addon_osx_arm64
    displayName: 'Addon osx-arm64'
    pool:
      name: Azure Pipelines
      vmImage: 'macOS-15'
      os: macOS
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-addon-darwin-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-addon'
    steps:
    - checkout: self
    - template: build-js-addon-steps.yml
      parameters:
        repoRoot: $(Build.SourcesDirectory)
        targetArch: arm64
    - task: PowerShell@2
      displayName: 'Stage addon artifact'
      inputs:
        targetType: inline
        script: |
          $destDir = "$(Build.ArtifactStagingDirectory)/js-addon"
          New-Item -ItemType Directory -Path $destDir -Force | Out-Null
          Copy-Item "$(Build.SourcesDirectory)/sdk/js/prebuilds/darwin-arm64/foundry_local_napi.node" "$destDir/foundry_local_napi.node" -Force

# ── Build JS SDK ──
- stage: build_js
  displayName: 'Build JS SDK'
  dependsOn:
  - v1_gate
  - build_core
  - build_js_addon
  jobs:
  - job: js_sdk
    displayName: 'Build'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-sdk'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-sdk'
    steps:
    - checkout: self
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared

    # Download prebuilt Node-API addons for all platforms
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (win32-x64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-win32-x64'
        targetPath: '$(Pipeline.Workspace)/js-addon-win32-x64'
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (win32-arm64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-win32-arm64'
        targetPath: '$(Pipeline.Workspace)/js-addon-win32-arm64'
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (linux-x64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-linux-x64'
        targetPath: '$(Pipeline.Workspace)/js-addon-linux-x64'
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (linux-arm64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-linux-arm64'
        targetPath: '$(Pipeline.Workspace)/js-addon-linux-arm64'
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (osx-arm64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-darwin-arm64'
        targetPath: '$(Pipeline.Workspace)/js-addon-darwin-arm64'

    - task: PowerShell@2
      displayName: 'Place prebuilt addons'
      inputs:
        targetType: inline
        script: |
          $multiCheckout = "$(Build.SourcesDirectory)/Foundry-Local"
          if (Test-Path $multiCheckout) { $repoRoot = $multiCheckout } else { $repoRoot = "$(Build.SourcesDirectory)" }
          $prebuildsDir = "$repoRoot/sdk/js/prebuilds"
          foreach ($platform in @('win32-x64','win32-arm64','linux-x64','linux-arm64','darwin-arm64')) {
            $src = "$(Pipeline.Workspace)/js-addon-$platform/foundry_local_napi.node"
            $destDir = "$prebuildsDir/$platform"
            New-Item -ItemType Directory -Path $destDir -Force | Out-Null
            Copy-Item $src "$destDir/foundry_local_napi.node" -Force
            Write-Host "Placed addon for $platform ($($(Get-Item $src).Length) bytes)"
          }

    - template: build-js-steps.yml
      parameters:
        version: ${{ parameters.version }}
        isRelease: ${{ parameters.isRelease }}
        prereleaseId: ${{ parameters.prereleaseId }}
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

# ── Build Python SDK ──
- stage: build_python
  displayName: 'Build Python SDK'
  dependsOn:
  - v1_gate
  - build_core
  jobs:
  - job: python_sdk
    displayName: 'Build'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'flc-wheels'
        targetPath: '$(Pipeline.Workspace)/flc-wheels'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
      outputs:
      - output: pipelineArtifact
        artifactName: 'python-sdk'
        targetPath: '$(Build.ArtifactStagingDirectory)/python-sdk'
    steps:
    - checkout: self
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-python-steps.yml
      parameters:
        version: ${{ parameters.version }}
        isRelease: ${{ parameters.isRelease }}
        prereleaseId: ${{ parameters.prereleaseId }}
        isWinML: false
        flcWheelsDir: '$(Pipeline.Workspace)/flc-wheels'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

# ── Build Rust SDK ──
- stage: build_rust
  displayName: 'Build Rust SDK'
  dependsOn:
  - v1_gate
  - build_core
  - build_core_winml
  jobs:
  - job: rust_sdk
    displayName: 'Build'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
      - input: pipelineArtifact
        artifactName: 'deps-versions-winml'
        targetPath: '$(Pipeline.Workspace)/deps-versions-winml'
      outputs:
      - output: pipelineArtifact
        artifactName: 'rust-sdk'
        targetPath: '$(Build.ArtifactStagingDirectory)/rust-sdk'
    steps:
    - checkout: self
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-rust-steps.yml
      parameters:
        version: ${{ parameters.version }}
        isRelease: ${{ parameters.isRelease }}
        prereleaseId: ${{ parameters.prereleaseId }}
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'
        depsVersionsWinmlDir: '$(Pipeline.Workspace)/deps-versions-winml'

# Rust SDK has one package with different install options for standard vs WinML, 
# so we only publish once under the standard stage and skip the WinML stage. Leaving 
# it as a commented block incase we decide to publish separate Rust WinML package in the future.
# # ── Build Rust SDK (WinML) ──
# - stage: build_rust_winml
#   displayName: 'Build Rust SDK (WinML)'
#   dependsOn:
#   - build_core_winml
#   jobs:
#   - job: rust_sdk_winml
#     displayName: 'Build'
#     pool:
#       name: onnxruntime-Win-CPU-2022
#       os: windows
#     templateContext:
#       inputs:
#       - input: pipelineArtifact
#         artifactName: 'version-info'
#         targetPath: '$(Pipeline.Workspace)/version-info'
#       - input: pipelineArtifact
#         artifactName: 'flc-nuget-winml'
#         targetPath: '$(Pipeline.Workspace)/flc-nuget-winml'
#       - input: pipelineArtifact
#         artifactName: 'deps-versions-winml'
#         targetPath: '$(Pipeline.Workspace)/deps-versions-winml'
#       outputs:
#       - output: pipelineArtifact
#         artifactName: 'rust-sdk-winml'
#         targetPath: '$(Build.ArtifactStagingDirectory)/rust-sdk-winml'
#     steps:
#     - checkout: self
#       clean: true
#     - checkout: test-data-shared
#       lfs: true
#     - template: build-rust-steps.yml
#       parameters:
#         version: ${{ parameters.version }}
#         isRelease: ${{ parameters.isRelease }}
#         prereleaseId: ${{ parameters.prereleaseId }}
#         isWinML: true
#         flcNugetDir: '$(Pipeline.Workspace)/flc-nuget-winml'
#         depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-winml'
#         outputDir: '$(Build.ArtifactStagingDirectory)/rust-sdk-winml'

# ── Test C# SDK ──
- stage: test_cs
  displayName: 'Test C#'
  dependsOn:
  - v1_gate
  - build_cs
  jobs:
  - job: test_cs_win_x64
    displayName: 'win-x64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
        basePath: '$(Agent.BuildDirectory)'
    - template: test-cs-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_cs_linux_x64
    displayName: 'linux-x64'
    pool:
      name: onnxruntime-Ubuntu2404-AMD-CPU
      os: linux
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
        basePath: '$(Agent.BuildDirectory)'
    - template: test-cs-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_cs_linux_arm64
    displayName: 'linux-arm64'
    pool:
      name: onnxruntime-linux-ARM64-CPU-2019
      os: linux
      hostArchitecture: arm64
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
        basePath: '$(Agent.BuildDirectory)'
    - template: test-cs-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_cs_osx_arm64
    displayName: 'osx-arm64'
    pool:
      name: AcesShared
      os: macOS
      demands:
      - ImageOverride -equals ACES_VM_SharedPool_Sequoia
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    # AcesShared macOS agents don't have git-lfs pre-installed
    - script: brew install git-lfs && git lfs install
      displayName: 'Install Git LFS'
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
        basePath: '$(Agent.BuildDirectory)'
    - template: test-cs-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

# ── Test JS SDK ──
- stage: test_js
  displayName: 'Test JS'
  dependsOn:
  - v1_gate
  - build_js
  jobs:
  - job: test_js_win_x64
    displayName: 'win-x64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-js-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_js_linux_x64
    displayName: 'linux-x64'
    pool:
      name: onnxruntime-Ubuntu2404-AMD-CPU
      os: linux
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
        basePath: '$(Agent.BuildDirectory)'
    - template: test-js-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_js_linux_arm64
    displayName: 'linux-arm64'
    pool:
      name: onnxruntime-linux-ARM64-CPU-2019
      os: linux
      hostArchitecture: arm64
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
        basePath: '$(Agent.BuildDirectory)'
    - template: test-js-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_js_osx_arm64
    displayName: 'osx-arm64'
    pool:
      name: AcesShared
      os: macOS
      demands:
      - ImageOverride -equals ACES_VM_SharedPool_Sequoia
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    # AcesShared macOS agents don't have git-lfs pre-installed
    - script: brew install git-lfs && git lfs install
      displayName: 'Install Git LFS'
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-js-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

# ── Test Python SDK ──
- stage: test_python
  displayName: 'Test Python'
  dependsOn:
  - v1_gate
  - build_python
  jobs:
  - job: test_python_win_x64
    displayName: 'win-x64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-wheels'
        targetPath: '$(Pipeline.Workspace)/flc-wheels'
      - input: pipelineArtifact
        artifactName: 'python-sdk'
        targetPath: '$(Pipeline.Workspace)/python-sdk'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-python-steps.yml
      parameters:
        isWinML: false
        flcWheelsDir: '$(Pipeline.Workspace)/flc-wheels'
        sdkWheelsDir: '$(Pipeline.Workspace)/python-sdk'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_python_linux_x64
    displayName: 'linux-x64'
    pool:
      name: onnxruntime-Ubuntu2404-AMD-CPU
      os: linux
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-wheels'
        targetPath: '$(Pipeline.Workspace)/flc-wheels'
      - input: pipelineArtifact
        artifactName: 'python-sdk'
        targetPath: '$(Pipeline.Workspace)/python-sdk'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
        basePath: '$(Agent.BuildDirectory)'
    - template: test-python-steps.yml
      parameters:
        isWinML: false
        flcWheelsDir: '$(Pipeline.Workspace)/flc-wheels'
        sdkWheelsDir: '$(Pipeline.Workspace)/python-sdk'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_python_linux_arm64
    displayName: 'linux-arm64'
    pool:
      name: onnxruntime-linux-ARM64-CPU-2019
      os: linux
      hostArchitecture: arm64
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-wheels'
        targetPath: '$(Pipeline.Workspace)/flc-wheels'
      - input: pipelineArtifact
        artifactName: 'python-sdk'
        targetPath: '$(Pipeline.Workspace)/python-sdk'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
        basePath: '$(Agent.BuildDirectory)'
    - template: test-python-steps.yml
      parameters:
        isWinML: false
        flcWheelsDir: '$(Pipeline.Workspace)/flc-wheels'
        sdkWheelsDir: '$(Pipeline.Workspace)/python-sdk'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_python_osx_arm64
    displayName: 'osx-arm64'
    pool:
      name: AcesShared
      os: macOS
      demands:
      - ImageOverride -equals ACES_VM_SharedPool_Sequoia
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-wheels'
        targetPath: '$(Pipeline.Workspace)/flc-wheels'
      - input: pipelineArtifact
        artifactName: 'python-sdk'
        targetPath: '$(Pipeline.Workspace)/python-sdk'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    # AcesShared macOS agents don't have git-lfs pre-installed
    - script: brew install git-lfs && git lfs install
      displayName: 'Install Git LFS'
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-python-steps.yml
      parameters:
        isWinML: false
        flcWheelsDir: '$(Pipeline.Workspace)/flc-wheels'
        sdkWheelsDir: '$(Pipeline.Workspace)/python-sdk'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

# ── Test Rust SDK ──
- stage: test_rust
  displayName: 'Test Rust'
  dependsOn:
  - v1_gate
  - build_rust
  jobs:
  - job: test_rust_win_x64
    displayName: 'win-x64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-rust-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_rust_linux_x64
    displayName: 'linux-x64'
    pool:
      name: onnxruntime-Ubuntu2404-AMD-CPU
      os: linux
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-rust-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_rust_linux_arm64
    displayName: 'linux-arm64'
    pool:
      name: onnxruntime-linux-ARM64-CPU-2019
      os: linux
      hostArchitecture: arm64
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-rust-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

  - job: test_rust_osx_arm64
    displayName: 'osx-arm64'
    pool:
      name: AcesShared
      os: macOS
      demands:
      - ImageOverride -equals ACES_VM_SharedPool_Sequoia
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget'
        targetPath: '$(Pipeline.Workspace)/flc-nuget'
      - input: pipelineArtifact
        artifactName: 'deps-versions-standard'
        targetPath: '$(Pipeline.Workspace)/deps-versions-standard'
    steps:
    # AcesShared macOS agents don't have git-lfs pre-installed
    - script: brew install git-lfs && git lfs install
      displayName: 'Install Git LFS'
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-rust-steps.yml
      parameters:
        isWinML: false
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-standard'

# ── Build FLC (WinML) ──
- stage: build_core_winml
  displayName: 'Build Core (WinML)'
  dependsOn:
  - v1_gate
  - compute_version
  jobs:
  - job: flc_winml_win_x64
    displayName: 'win-x64 (WinML)'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'flc-winml-win-x64'
        targetPath: '$(Build.ArtifactStagingDirectory)/native'
    steps:
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: neutron-server
        ref: refs/heads/${{ parameters.neutronServerBranch }}
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-core-steps.yml
      parameters:
        flavor: win-x64
        platform: x64
        isWinML: true

  - job: flc_winml_win_arm64
    displayName: 'win-arm64 (WinML)'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      outputs:
      - output: pipelineArtifact
        artifactName: 'flc-winml-win-arm64'
        targetPath: '$(Build.ArtifactStagingDirectory)/native'
    steps:
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: neutron-server
        ref: refs/heads/${{ parameters.neutronServerBranch }}
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-core-steps.yml
      parameters:
        flavor: win-arm64
        platform: arm64
        isWinML: true

  - job: package_flc_winml
    displayName: 'Package Core (WinML)'
    dependsOn:
    - flc_winml_win_x64
    - flc_winml_win_arm64
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      outputs:
      - output: pipelineArtifact
        artifactName: 'flc-nuget-winml'
        targetPath: '$(Build.ArtifactStagingDirectory)/flc-nuget'
      - output: pipelineArtifact
        artifactName: 'flc-wheels-winml'
        targetPath: '$(Build.ArtifactStagingDirectory)/flc-wheels'
      - output: pipelineArtifact
        artifactName: 'deps-versions-winml'
        targetPath: '$(Build.ArtifactStagingDirectory)/deps-versions'
    steps:
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: neutron-server
        ref: refs/heads/${{ parameters.neutronServerBranch }}
    - task: DownloadPipelineArtifact@2
      inputs:
        buildType: current
        artifactName: 'flc-winml-win-x64'
        targetPath: '$(Pipeline.Workspace)/flc-winml-win-x64'
    - task: DownloadPipelineArtifact@2
      inputs:
        buildType: current
        artifactName: 'flc-winml-win-arm64'
        targetPath: '$(Pipeline.Workspace)/flc-winml-win-arm64'
    - task: PowerShell@2
      displayName: 'List downloaded WinML platform artifacts'
      inputs:
        targetType: inline
        script: |
          foreach ($name in @('flc-winml-win-x64','flc-winml-win-arm64')) {
            $dir = "$(Pipeline.Workspace)/$name"
            Write-Host "Contents of ${dir}:"
            if (Test-Path $dir) { Get-ChildItem $dir -Recurse | ForEach-Object { Write-Host $_.FullName } }
            else { Write-Host "  (directory not found)" }
          }
    - template: package-core-steps.yml
      parameters:
        version: ${{ parameters.version }}
        isRelease: ${{ parameters.isRelease }}
        prereleaseId: ${{ parameters.prereleaseId }}
        isWinML: true
        platforms:
        - name: win-x64
          artifactName: flc-winml-win-x64
        - name: win-arm64
          artifactName: flc-winml-win-arm64

# ── Build C# SDK (WinML) ──
- stage: build_cs_winml
  displayName: 'Build C# SDK (WinML)'
  dependsOn:
  - v1_gate
  - build_core_winml
  jobs:
  - job: cs_sdk_winml
    displayName: 'Build'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'flc-nuget-winml'
        targetPath: '$(Pipeline.Workspace)/flc-nuget-winml'
      - input: pipelineArtifact
        artifactName: 'deps-versions-winml'
        targetPath: '$(Pipeline.Workspace)/deps-versions-winml'
      outputs:
      - output: pipelineArtifact
        artifactName: 'cs-sdk-winml'
        targetPath: '$(Build.ArtifactStagingDirectory)/cs-sdk-winml'
    steps:
    - checkout: self
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-cs-steps.yml
      parameters:
        version: ${{ parameters.version }}
        isRelease: ${{ parameters.isRelease }}
        prereleaseId: ${{ parameters.prereleaseId }}
        isWinML: true
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget-winml'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-winml'
        outputDir: '$(Build.ArtifactStagingDirectory)/cs-sdk-winml'

# ── Build JS SDK (WinML) ──
- stage: build_js_winml
  displayName: 'Build JS SDK (WinML)'
  dependsOn:
  - v1_gate
  - build_core_winml
  - build_js_addon
  jobs:
  - job: js_sdk_winml
    displayName: 'Build'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'flc-nuget-winml'
        targetPath: '$(Pipeline.Workspace)/flc-nuget-winml'
      - input: pipelineArtifact
        artifactName: 'deps-versions-winml'
        targetPath: '$(Pipeline.Workspace)/deps-versions-winml'
      outputs:
      - output: pipelineArtifact
        artifactName: 'js-sdk-winml'
        targetPath: '$(Build.ArtifactStagingDirectory)/js-sdk'
    steps:
    - checkout: self
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared

    # Download prebuilt Node-API addons for all platforms
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (win32-x64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-win32-x64'
        targetPath: '$(Pipeline.Workspace)/js-addon-win32-x64'
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (win32-arm64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-win32-arm64'
        targetPath: '$(Pipeline.Workspace)/js-addon-win32-arm64'
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (linux-x64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-linux-x64'
        targetPath: '$(Pipeline.Workspace)/js-addon-linux-x64'
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (linux-arm64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-linux-arm64'
        targetPath: '$(Pipeline.Workspace)/js-addon-linux-arm64'
    - task: DownloadPipelineArtifact@2
      displayName: 'Download addon (osx-arm64)'
      inputs:
        buildType: current
        artifactName: 'js-addon-darwin-arm64'
        targetPath: '$(Pipeline.Workspace)/js-addon-darwin-arm64'

    - task: PowerShell@2
      displayName: 'Place prebuilt addons'
      inputs:
        targetType: inline
        script: |
          $multiCheckout = "$(Build.SourcesDirectory)/Foundry-Local"
          if (Test-Path $multiCheckout) { $repoRoot = $multiCheckout } else { $repoRoot = "$(Build.SourcesDirectory)" }
          $prebuildsDir = "$repoRoot/sdk/js/prebuilds"
          foreach ($platform in @('win32-x64','win32-arm64','linux-x64','linux-arm64','darwin-arm64')) {
            $src = "$(Pipeline.Workspace)/js-addon-$platform/foundry_local_napi.node"
            $destDir = "$prebuildsDir/$platform"
            New-Item -ItemType Directory -Path $destDir -Force | Out-Null
            Copy-Item $src "$destDir/foundry_local_napi.node" -Force
            Write-Host "Placed addon for $platform ($($(Get-Item $src).Length) bytes)"
          }

    - template: build-js-steps.yml
      parameters:
        version: ${{ parameters.version }}
        isRelease: ${{ parameters.isRelease }}
        prereleaseId: ${{ parameters.prereleaseId }}
        isWinML: true
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget-winml'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-winml'

# ── Build Python SDK (WinML) ──
- stage: build_python_winml
  displayName: 'Build Python SDK (WinML)'
  dependsOn:
  - v1_gate
  - build_core_winml
  jobs:
  - job: python_sdk_winml
    displayName: 'Build'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'version-info'
        targetPath: '$(Pipeline.Workspace)/version-info'
      - input: pipelineArtifact
        artifactName: 'flc-wheels-winml'
        targetPath: '$(Pipeline.Workspace)/flc-wheels-winml'
      - input: pipelineArtifact
        artifactName: 'deps-versions-winml'
        targetPath: '$(Pipeline.Workspace)/deps-versions-winml'
      outputs:
      - output: pipelineArtifact
        artifactName: 'python-sdk-winml'
        targetPath: '$(Build.ArtifactStagingDirectory)/python-sdk-winml'
    steps:
    - checkout: self
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: build-python-steps.yml
      parameters:
        version: ${{ parameters.version }}
        isRelease: ${{ parameters.isRelease }}
        prereleaseId: ${{ parameters.prereleaseId }}
        isWinML: true
        flcWheelsDir: '$(Pipeline.Workspace)/flc-wheels-winml'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-winml'
        outputDir: '$(Build.ArtifactStagingDirectory)/python-sdk-winml'

# ── Test C# SDK (WinML) ──
- stage: test_cs_winml
  displayName: 'Test C# (WinML)'
  dependsOn:
  - v1_gate
  - build_cs_winml
  jobs:
  - job: test_cs_winml_win_x64
    displayName: 'win-x64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget-winml'
        targetPath: '$(Pipeline.Workspace)/flc-nuget-winml'
      - input: pipelineArtifact
        artifactName: 'deps-versions-winml'
        targetPath: '$(Pipeline.Workspace)/deps-versions-winml'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
        basePath: '$(Agent.BuildDirectory)'
    - template: test-cs-steps.yml
      parameters:
        isWinML: true
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget-winml'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-winml'

# ── Test JS SDK (WinML) ──
- stage: test_js_winml
  displayName: 'Test JS (WinML)'
  dependsOn:
  - v1_gate
  - build_js_winml
  jobs:
  - job: test_js_winml_win_x64
    displayName: 'win-x64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-nuget-winml'
        targetPath: '$(Pipeline.Workspace)/flc-nuget-winml'
      - input: pipelineArtifact
        artifactName: 'deps-versions-winml'
        targetPath: '$(Pipeline.Workspace)/deps-versions-winml'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-js-steps.yml
      parameters:
        isWinML: true
        flcNugetDir: '$(Pipeline.Workspace)/flc-nuget-winml'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-winml'

# ── Test Python SDK (WinML) ──
- stage: test_python_winml
  displayName: 'Test Python (WinML)'
  dependsOn:
  - v1_gate
  - build_python_winml
  jobs:
  - job: test_python_winml_win_x64
    displayName: 'win-x64'
    pool:
      name: onnxruntime-Win-CPU-2022
      os: windows
    templateContext:
      inputs:
      - input: pipelineArtifact
        artifactName: 'flc-wheels-winml'
        targetPath: '$(Pipeline.Workspace)/flc-wheels-winml'
      - input: pipelineArtifact
        artifactName: 'python-sdk-winml'
        targetPath: '$(Pipeline.Workspace)/python-sdk-winml'
      - input: pipelineArtifact
        artifactName: 'deps-versions-winml'
        targetPath: '$(Pipeline.Workspace)/deps-versions-winml'
    steps:
    - checkout: self
      clean: true
    - template: ../../templates/checkout-steps.yml@self
      parameters:
        repoName: test-data-shared
    - template: test-python-steps.yml
      parameters:
        isWinML: true
        flcWheelsDir: '$(Pipeline.Workspace)/flc-wheels-winml'
        sdkWheelsDir: '$(Pipeline.Workspace)/python-sdk-winml'
        depsVersionsDir: '$(Pipeline.Workspace)/deps-versions-winml'