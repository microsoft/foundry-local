# Foundry Local Website - Environment Variables Example
# Copy this file to .env and fill in your values

# Vercel Analytics (optional)
# Get from: https://vercel.com/docs/analytics
# VERCEL_ANALYTICS_ID=your_analytics_id_here

# Public API Endpoint (already configured in code)
# This is the Azure Function proxy endpoint for CORS
# PUBLIC_FOUNDRY_API_ENDPOINT=https://foundry-local-cors-proxy.azurewebsites.net/api/foundryproxy

# Build-time variables
# NODE_ENV=production

# Deployment platform (Vercel)
# These are typically set automatically by Vercel
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=foundrylocal.ai

# SvelteKit
# PUBLIC_BASE_URL=https://foundrylocal.ai

# Note: This project currently doesn't require any sensitive environment variables
# All API endpoints are public and configured directly in the code
